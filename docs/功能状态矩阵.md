# 功能状态矩阵

更新时间：2026-04-28

本矩阵用于约束文档声明，避免把占位、模拟或实验性能力描述为生产可用能力。状态定义如下：

| 状态 | 含义 | 发布说明要求 |
| --- | --- | --- |
| 已实现 | 有源码主链路、自动化测试或可复现手动验收记录支撑 | 可以写入主要功能说明 |
| 实验性 | 有主链路或框架，但缺少真实硬件、真实样本或跨平台验证 | 必须标注实验性和限制 |
| 模拟 | 依赖 mock、示例数据、固定返回值或占位 UI | 不得写成真实硬件能力 |
| 规划中 | 尚未接入主链路 | 只能出现在路线图或待办 |

| 功能域 | 当前状态 | 证据 | 下一步 |
| --- | --- | --- | --- |
| VSCode 扩展入口和 `.lac` 自定义编辑器 | 已实现 | `package.json` 命令与 custom editor 注册；`src/providers/LACEditorProvider.ts` | 项目 04 继续补齐激活事件和 host 命令链路 |
| Vue3 Webview 布局 | 已实现 | `src/webview/App.vue` 已转接 `src/frontend/app/App.vue`；`test:webview:unit` 41 项通过 | 项目 04/06 继续强化 host 命令和复杂交互 |
| Pico 驱动基础协议 | 实验性 | `src/drivers/LogicAnalyzerDriver.ts` 已具备启动确认、停止、blink、blast、limit、网络配置和协议 fixture 覆盖；真实硬件记录仍缺失 | 项目 01 补真实 Pico / Pico W / Pico 2 手动验收记录 |
| 串口真实采集 | 实验性 | 已有 `tests/fixtures/drivers/pico-serial-8ch-single-frame.json` 覆盖二进制帧回放和文本 parser 暂停/恢复；真实硬件未验证 | 项目 01 补真实硬件采集记录 |
| 网络设备发现 | 实验性 | 网络扫描服务存在，但端口和协议未与原版统一 | 项目 07 统一设备识别和认证 |
| Saleae / Rigol / Siglent / sigrok 扩展 | 实验性 | 驱动框架存在，真实设备兼容证据不足 | 补硬件认证矩阵和 golden 转换测试 |
| I2C / SPI / UART 解码器 | 已实现 | `src/decoders/protocols/` 与单元测试目录 | 项目 05 补 sigrok golden 对齐 |
| 其他 100+ sigrok 协议 | 规划中 | 当前未提供等价 TypeScript 解码器 | 按 CAN、LIN、I2S、USB 等优先级补齐 |
| `.lac` 保存与 CSV/JSON/VCD 导出 | 实验性 | `tests/fixtures/lac/` 已补 PascalCase/lowercase golden；`LACFileFormat.test.ts` 覆盖原版打开、当前保存、区域恢复和往返；`DataExportService.accurate.test.ts` 覆盖 LAC/CSV/JSON/VCD 导出样本语义 | 补真实原版 GUI 导出的大样本和跨版本 hash 记录后提升状态 |
| Webview 导出按钮 | 实验性 | `LACEditorProvider.exportData()` 已接入 `DataExportService`，不再只是提示成功 | 项目 02 补导出格式和错误路径测试 |
| 新前端真实波形装载 | 已实现 | `sessionStore` 可解析 root/channel 大小写样本并推导样本数；`AppWaveformStage` 覆盖 empty/settings-only/invalid/samples 状态；`WaveformRenderer` 有真实样本非空渲染回归；`test:webview:unit` 41 项通过 | 合并前补真实浏览器截图 smoke；复杂编辑归项目 06 |
| 区域、marker、测量、样本编辑 | 实验性 | 新前端 store 和 stage 已有基础操作，仍缺少完整原版工作流和持久化 | 项目 06 补测试、回写和性能边界 |
| CLI / TUI 采集工具 | 实验性 | `package.json` 已注册 `logic-analyzer-capture`，`src/cli` 和 CLI 单元测试存在 | 项目 08 对齐原版 CLCapture/TerminalCapture |
| 信号描述语言和合成捕获 | 实验性 | `SignalDescriptionLanguage.ts` 与 `logicAnalyzer.createSyntheticCapture` 命令存在 | 项目 09 补 DSL 语法、诊断和样本生成 fixture |
| 质量门禁和 CI | 已实现 | `typecheck`、`typecheck:strict`、`test:webview:unit`、`build:production` 当前通过；CI 含 typecheck/strict/lint/quick/webview/driver/decoder/package dry run | 项目 10 继续拆分长耗时单元测试并归档旧文档 |
