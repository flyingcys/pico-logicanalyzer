# 功能状态矩阵

更新时间：2026-04-30

本矩阵用于约束文档声明，避免把占位、模拟或实验性能力描述为生产可用能力。状态定义如下：

| 状态 | 含义 | 发布说明要求 |
| --- | --- | --- |
| 已实现 | 有源码主链路、自动化测试或可复现手动验收记录支撑 | 可以写入主要功能说明 |
| 实验性 | 有主链路或框架，但缺少真实硬件、真实样本或跨平台验证 | 必须标注实验性和限制 |
| 模拟 | 依赖 mock、示例数据、固定返回值或占位 UI | 不得写成真实硬件能力 |
| 规划中 | 尚未接入主链路 | 只能出现在路线图或待办 |

| 功能域 | 当前状态 | 证据 | 下一步 |
| --- | --- | --- | --- |
| VSCode 扩展入口和 `.lac` 自定义编辑器 | 已实现 | `package.json` 命令与 custom editor 注册；`src/providers/LACEditorProvider.ts`；04 已补齐设备/采集 host command request-response | 合并后继续由各硬件分支补真实协议和认证记录 |
| Vue3 Webview 布局 | 已实现 | `src/webview/App.vue` 已转接 `src/frontend/app/App.vue`；`npm run test:webview:unit -- --runInBand` 通过，2 个套件、69 个测试 | 继续补真实浏览器 smoke 和复杂交互验收 |
| Pico 驱动基础协议 | 实验性 | `src/drivers/LogicAnalyzerDriver.ts` 已具备启动确认、停止、blink、blast、limit、网络配置和协议 fixture 覆盖；真实硬件记录仍缺失 | 项目 01 补真实 Pico / Pico W / Pico 2 手动验收记录 |
| 串口真实采集 | 实验性 | 已有 `tests/fixtures/drivers/pico-serial-8ch-single-frame.json` 覆盖二进制帧回放和文本 parser 暂停/恢复；真实硬件未验证 | 项目 01 补真实硬件采集记录 |
| 网络设备发现 | 实验性 | 已统一发现 profile；Pico TCP 端口 `4045` 需要版本握手，端口开放只作为候选 | 后续把真实握手探测接入扫描循环并补真实 Pico W 记录 |
| Saleae / Rigol / Siglent / sigrok 扩展 | 实验性 | 适配框架和检测口径存在；认证矩阵已把 framework 与硬件认证分开 | 按设备型号补真实采集记录和 golden 转换测试 |
| I2C / SPI / UART / CAN / LIN / I2S 解码器 | 已实现 | `src/decoders/protocols/`、`tests/fixtures/decoders/sigrok-golden.json` 覆盖正常/错误/边界 golden；`npm run test:decoders -- --runInBand` 通过 | 继续用真实 sigrok 输出扩充更长事务、真实采样和时序边界 |
| I2C 协议解码 UI 闭环 | 已实现 | `tests/fixtures/lac/i2c-ui-smoke.lac.json` 提供 CH0/SCL、CH1/SDA 的最小事务；`src/providers/LACEditorProvider.ts` 已接入 `runDecoder` host command，右侧 I2C 解码 UI 和 browser smoke 覆盖 START/ACK/STOP 软件路径；当前证据仍限于 fixture 和自动化 smoke，未包含真实 Pico 采集记录 | 按 `docs/hardware-validation/i2c-pico-ui-manual-test.md` 补真实 Pico + I2C 目标设备的 `.lac` hash、截图和认证记录后，再提升真实硬件状态 |
| I2S 协议解码 UI 闭环 | 已实现 | 右侧协议解码面板支持选择 I2S、配置 SCK/WS/SD 映射、`word_length` 和 `justification`；`decoderStore.runSelectedDecoder()` 可发送 `runDecoder('i2s')`；VSCode host 走真实 `DecoderManager.executeDecoder()`，HTML host 仅提供 `I2S HTML 模拟` smoke；`tests/unit/decoders/protocols/I2SDecoder.test.ts` 覆盖 32 位高位、WS 边界、多余 bit warning、首字同步、实例复用和 Manager 映射；`tests/unit/webview/main.test.ts` 覆盖 I2S UI/store/browser-host 路径 | 当前 I2S UI 可作为软件手工测试候选；真实硬件通过必须按 `docs/hardware-validation/i2s-pico-ui-manual-test.md` 记录设备、`.lac` sha256、截图和结论。I2S 当前只声明 annotation 样本输出，binary 输出和 WAV 导出不支持 |
| 其他 100+ sigrok 协议 | 规划中 | `src/decoders/DecoderSigrokParity.ts` 保留 USB、JTAG/SWD 等高复杂协议的外部 sigrok 过渡策略；CAN/LIN/I2S 已进入本地 TypeScript 核心解码器范围 | USB/JTAG-SWD 暂走外部 sigrok 过渡，不声明默认本地支持 |
| `.lac` 保存与 CSV/JSON/VCD 导出 | 实验性 | `tests/fixtures/lac/` 已补 PascalCase/lowercase golden；`LACFileFormat.test.ts` 覆盖原版打开、当前保存、区域恢复和往返；`DataExportService.accurate.test.ts` 覆盖 LAC/CSV/JSON/VCD 导出样本语义 | 补真实原版 GUI 导出的大样本和跨版本 hash 记录后提升状态 |
| Webview 导出按钮 | 实验性 | `LACEditorProvider.exportData()` 已接入 `DataExportService`，当前主链路支持 LAC/CSV/JSON/VCD；导出通道校验已按实际 `channelNumber` 集合处理 | 继续补取消路径、错误路径、选区/区域导出和真实文件 smoke |
| 新前端真实波形装载 | 已实现 | `sessionStore` 可解析 root/channel 大小写样本并推导样本数；`AppWaveformStage` 覆盖 empty/settings-only/invalid/samples 状态；`WaveformRenderer` 有真实样本非空渲染回归；I2C fixture smoke 使用 root `Samples` 验证波形 canvas 路径；`npm run test:webview:unit -- --runInBand` 通过，2 个套件、51 个测试 | 补真实浏览器截图 smoke；复杂编辑继续保持独立验收 |
| Webview 设备连接和采集 UI | 实验性 | `AppHeader.vue`、`AppSidebarLeft.vue`、`deviceCaptureCommands.ts` 和 `LACEditorProvider.executeHostCommand()` 已接通连接、扫描、开始、重复、停止采集；`test:webview:unit` 覆盖成功和错误路径 | 等 01/07 提供真实 Pico 协议 fixture 和硬件认证记录后提升真实硬件状态 |
| 区域、marker、测量、样本编辑 | 已实现 | `waveformStore` 覆盖 selection、region、marker、频率/占空比/脉宽、复制/剪切/插入/覆盖粘贴/删除、撤销/重做；`waveform-interactions.test.ts` 覆盖成功和边界路径 | 与 02/04 合并时把 `exportSelectedRegionsForLac()` 接入实际保存链路 |
| CLI / TUI 采集工具 | 实验性 | `package.json` 已注册 `logic-analyzer-capture`；`src/cli` 支持 mock/真实 runner、`.lac`/`.csv`/`.json`、repeat、batch 和错误码；`npm run test:cli` 覆盖主链路 | 真实硬件采集仍需项目 01/07 的协议和认证记录；完整 TUI 后续再接 |
| 信号描述语言和合成捕获 | 实验性 | `SignalDescriptionLanguage.ts` 支持 DSL 解析、稳定诊断和样本生成；`tests/fixtures/signal-dsl/` 覆盖 I2C/SPI/UART/CAN 示例；`logicAnalyzer.createSyntheticCapture` 可保存含根级 `Samples` 的 `.lac` | 继续扩展多错误诊断和协议帧级 AST，合并 02 后复核 `.lac` golden |
| 质量门禁和 CI | 已实现 | `typecheck`、`typecheck:strict`、`lint`、`test:webview:unit`、`test:ci:quick/standard/full -- --skip-install`、`package:dry` 当前通过；Quick 14 文件 373 测试，Standard 18 文件 383 测试，Full 22 文件 393 测试 | 继续拆分长耗时单元测试、降低对 `--forceExit` 的依赖，并归档旧文档 |
| HTML/PDF/ZIP/项目包导出 | 实验性 | `DataExportService` 暴露相关接口，但 Webview 主入口尚未打通；PDF 为占位内容，HTML 图表仍是占位块；SDK 文档输出的 `html/pdf` 选项当前只写 Markdown 文本到对应扩展名 | 发布说明只能声明 `.lac/CSV/JSON/VCD` 主链路；报告、ZIP 和项目包导出需补真实实现和测试后再提升状态 |
