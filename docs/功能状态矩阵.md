# 功能状态矩阵

更新时间：2026-04-27

本矩阵用于约束文档声明，避免把占位、模拟或实验性能力描述为生产可用能力。状态定义如下：

| 状态 | 含义 | 发布说明要求 |
| --- | --- | --- |
| 已实现 | 有源码主链路、自动化测试或可复现手动验收记录支撑 | 可以写入主要功能说明 |
| 实验性 | 有主链路或框架，但缺少真实硬件、真实样本或跨平台验证 | 必须标注实验性和限制 |
| 模拟 | 依赖 mock、示例数据、固定返回值或占位 UI | 不得写成真实硬件能力 |
| 规划中 | 尚未接入主链路 | 只能出现在路线图或待办 |

| 功能域 | 当前状态 | 证据 | 下一步 |
| --- | --- | --- | --- |
| VSCode 扩展入口和 `.lac` 自定义编辑器 | 已实现 | `package.json` 命令与 custom editor 注册；`src/providers/LACEditorProvider.ts` | 由项目 02 接入统一 `.lac` 导入导出 |
| Vue3 Webview 布局 | 已实现 | `src/webview/App.vue` 已转接 `src/frontend/app/App.vue` | 由项目 03/06 接回真实波形和交互 |
| Pico 驱动基础协议 | 实验性 | `src/drivers/LogicAnalyzerDriver.ts` 与文档差距第 5 节 | 等待项目 01 修正采集状态机和字节级 fixture |
| 串口真实采集 | 实验性 | 存在 `ReadlineParser` 与二进制流共用风险 | 项目 01 提供 mock 二进制回放和真实硬件记录 |
| 网络设备发现 | 实验性 | 网络扫描服务存在，但端口和协议未与原版统一 | 项目 07 统一设备识别和认证 |
| Saleae / Rigol / Siglent / sigrok 扩展 | 实验性 | 驱动框架存在，真实设备兼容证据不足 | 补硬件认证矩阵和 golden 转换测试 |
| I2C / SPI / UART 解码器 | 已实现 | `src/decoders/protocols/` 与单元测试目录 | 项目 05 补 sigrok golden 对齐 |
| 其他 100+ sigrok 协议 | 规划中 | 当前未提供等价 TypeScript 解码器 | 按 CAN、LIN、I2S、USB 等优先级补齐 |
| `.lac` 保存与 CSV 导出 | 实验性 | 多套模型和大小写差异仍存在 | 项目 02 统一 PascalCase 格式和真实 fixture |
| Webview 导出按钮 | 模拟 | `LACEditorProvider.exportData()` 仍有 TODO 风险 | 项目 02 接入 `DataExportService` |
| 新前端真实波形装载 | 实验性 | 当前主要保存布局和摘要状态 | 项目 03 接入真实 channel data |
| 区域、marker、测量、样本编辑 | 规划中 | 旧引擎有能力，新前端未形成工作流 | 项目 06 迁移并补测试 |
| CLI / TUI 采集工具 | 规划中 | 仅有开发脚本，无用户级采集命令 | 项目 08 实现 `logic-analyzer-capture` |
| 信号描述语言和合成捕获 | 规划中 | 当前没有 DSL parser 和用户入口 | 项目 09 实现 AST、诊断和样本生成 |
| 质量门禁和 CI | 已实现 | `.github/workflows/quality-gate.yml`、`tsconfig.strict.json`、`.husky/pre-commit`；strict gate 当前覆盖核心类型模块 | 持续扩大 strict 覆盖面 |
