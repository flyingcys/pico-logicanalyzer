# 当前实现与文档对齐审计

更新时间：2026-05-02

本文档用于记录本次对当前项目代码、当前有效文档、发布口径和测试门禁的交叉审计结果。重点不是重复列出所有待办，而是明确：

- 当前代码已经落地的能力
- 当前有效文档与实现是否一致
- 已经开发、但仍未完成“状态对齐 / 证据对齐 / 发布对齐”的工作

本审计不新增需求，不扩展范围；只整理已经存在但尚未完全对齐的工作。

## 审计范围

- 代码入口：`src/extension.ts`、`src/providers/LACEditorProvider.ts`
- 前端主链路：`src/frontend/app/*`、`src/frontend/core/stores/*`、`src/frontend/platform/host/*`
- 导出链路：`src/services/DataExportService.ts`、`src/frontend/core/services/exportRequestService.ts`
- 解码链路：`src/decoders/*`、`tests/unit/decoders/*`
- 当前有效文档：`README.md`、`docs/功能状态矩阵.md`、`docs/真实硬件认证矩阵.md`、`docs/release-gate.md`、`docs/tests/README.md`、`docs/文档状态索引.md`
- 近期状态基线：`docs/superpowers/plans/2026-04-30-current-status-review-and-next-work.md`
- 近期增量提交：`bfb7e22655e8b038e1ad45e37b7e0971741b97a6`（2026-05-01）

## 本次重新验证结果

以下命令已于 2026-05-02 在当前工作区重新执行：

| 命令 | 结果 | 结论 |
| --- | --- | --- |
| `rtk npm run typecheck` | 通过 | 基础类型门禁仍有效 |
| `rtk npm run typecheck:strict` | 通过 | 分阶段 strict gate 仍有效 |
| `rtk npm run lint` | 通过 | 当前源码 lint 基线成立 |
| `rtk npm run test:webview:unit -- --runInBand` | 通过 | 3 个套件、98 个测试 |
| `rtk npm run test:decoders -- --runInBand` | 通过 | 10 个套件、196 个测试 |
| `rtk npm exec -- jest tests/unit/extension/LACEditorProvider.export.test.ts --runInBand` | 通过 | 导出用户路径补强已落地 |
| `rtk npm exec -- jest tests/unit/services/DataExportService.accurate.test.ts --runInBand` | 通过 | 导出样本范围、通道校验和错误路径已覆盖 |
| `rtk npm exec -- jest tests/unit/webview/decoderMappingHelpers.test.ts --runInBand` | 通过 | 映射 helper 收口已落地 |
| `rtk npm exec -- jest tests/unit/quality/QualityGateConfig.test.ts --runInBand` | 通过 | 文档/门禁/清单约束仍有效 |
| `rtk npm run test:ci:quick -- --skip-install` | 通过 | 14 个文件、373 个测试 |
| `rtk npm run test:ci:standard -- --skip-install` | 通过 | 18 个文件、383 个测试 |
| `rtk npm run test:ci:full -- --skip-install` | 通过 | 22 个文件、393 个测试 |
| `rtk npm run package:dry` | 通过 | VSIX 清单链路仍有效 |
| `rtk npm run release:check` | 通过 | 56 项检查，0 失败，3 warning |

结论：`docs/superpowers/plans/2026-04-30-current-status-review-and-next-work.md` 中的软件基线结论在 2026-05-02 仍成立，但需要补充 2026-05-01 的增量收口内容，并把日期、口径和对齐清单更新到当前有效文档。

## 当前已确认对齐的实现

以下能力已不再只是“框架存在”，而是有代码与测试共同支撑：

| 能力 | 当前实现证据 | 当前口径 |
| --- | --- | --- |
| `.lac` 自定义编辑器入口 | `package.json` custom editor、`src/providers/LACEditorProvider.ts` | Beta |
| Vue3 Webview 主界面 | `src/webview/App.vue` 转接 `src/frontend/app/App.vue`；Webview unit 98 测试 | Beta / fixture |
| I2C / SPI / UART / CAN / LIN / I2S 解码器 | `src/decoders/protocols/*`；decoders 196 测试 | fixture |
| 协议解码 UI 闭环 | `runDecoder` host command、右侧协议面板、browser/webview 测试 | fixture / experimental |
| Webview 导出主链路 | `LACEditorProvider.exportData()`、`DataExportService.exportWaveformData()`、专项导出测试 | experimental |
| 导出范围、通道校验与 `.lac SelectedRegions` 回填 | `exportRequestService`、`LACEditorProvider.exportData()`、`DataExportService.accurate.test.ts`、`LACEditorProvider.export.test.ts` | 已实现但仍属软件证据 |
| I2C 自动映射收口 | `decoderMappingHelpers.ts`、`decoderStore.ts`、专项测试 | 已实现 |
| 分层质量门禁 | `scripts/ci-test-runner.js`、`tests/unit/quality/*`、fresh quick/standard/full 结果 | Beta |

## 当前仍未对齐的问题类型

本次审计把“未对齐”分成 4 类，避免混淆：

| 类型 | 含义 | 当前是否存在 |
| --- | --- | --- |
| 状态口径未对齐 | 文档日期、状态等级或汇总口径落后于代码/测试 | 是 |
| 证据未对齐 | 代码已实现，但缺真实设备、GUI smoke、原版样本等外部证据 | 是 |
| 入口文档未对齐 | 存在重复文档或旧副本，容易误导读者 | 是 |
| 代码注释未对齐 | 源码注释仍使用“100%兼容”“完全兼容”等过强表述 | 是 |

## 已开发但未完成对齐的工作清单

以下只列已经开发过、但截至 2026-05-02 仍未完成对齐的项目。

### 1. VSIX 发布 smoke 记录只完成自动侧，GUI 侧仍未对齐

当前状态：

- `docs/release-smoke/2026-04-30-vsix-smoke.md` 已记录 VSIX 文件、大小、sha256、平台、CLI 版本
- `npm run package` 与 `npm run package:dry` 已有证据
- 干净 VSCode 安装、自定义编辑器打开、无设备错误路径、合成采集命令、卸载残留检查仍为 `pending`

说明：

- 这不是“没有做发布工作”，而是“发布证据只做了一半”
- 当前代码与脚本已经为 smoke 记录准备好模板和自动字段，但最终发布对齐仍缺桌面 GUI 证据

### 2. 协议解码 UI 已闭环，但真实硬件证据仍未对齐

当前状态：

- I2C / SPI / UART / CAN / LIN / I2S UI 软件路径已存在
- `runDecoder`、映射、选项、结果展示和 browser/webview smoke 已覆盖
- `docs/hardware-validation/*.md` 已有手工模板
- `docs/hardware-validation/records/` 仍只有 pending 记录

说明：

- 这不是“协议 UI 未完成”
- 真正未完成的是“从软件闭环提升到真实设备闭环”的证据对齐

### 3. Webview 导出按钮主链路已继续收口，但发布级证据仍未完全对齐

当前状态：

- `LACEditorProvider.exportData()` 已限制主链路为 `lac/csv/json/vcd`
- Webview `selection/visible/custom` 已转成显式样本范围
- `.lac` 导出时已可把 Webview `selectedRegions` 回填为 `SelectedRegions`
- 导出取消、失败、对象错误、通道号校验、半开区间处理已有测试

仍未对齐的部分：

- 真实文件 smoke 仍未补
- 选区/区域导出虽然已有范围处理和 `.lac` 区域回填，但与手工路径、真实文件结果之间还没有完整发布级证据
- 当前有效文档需要持续强调：用户主链路只有 `LAC/CSV/JSON/VCD`

### 4. `HTML/PDF/ZIP/项目包导出` 有实现入口，但状态仍未对齐到“不可宣称完成”

当前状态：

- `DataExportService` 已暴露 `html/pdf/zip/project` 相关接口
- HTML 报告能生成字符串结果，但图表区是占位块
- PDF 仍是明确占位实现
- ZIP / 项目包可以导出结构化内容，但尚无真实导入/回放/发布级验证

说明：

- 这部分不是“完全没做”
- 但离“可对外宣称主能力”还差真实生成链路、回放验证、UI 主入口和 smoke 证据

### 5. strict gate 已继续扩围，但全仓 strict 仍未对齐

当前状态：

- `tsconfig.strict.json` 已覆盖 `models`、`decoders` 和少量 `driver/service/frontend core` 入口
- 2026-05-02 已新增 `src/frontend/core/services/exportRequestService.ts`、`src/frontend/core/stores/sessionStore.ts`、`src/frontend/core/stores/waveformStore.ts`
- 对应命令今天重新验证通过

仍未对齐的部分：

- 主 `tsconfig.json` 仍非 strict
- 大量 driver/service/frontend 文件未纳入 strict 入口
- 当前只能声明“分阶段 strict gate”，不能声明“strict TypeScript 迁移完成”

### 6. 发布 smoke 的自动侧已前进，但 GUI 侧仍受环境阻断

当前状态：

- `standard` 与 `full` 已在 2026-05-02 fresh 复验通过
- 已尝试执行 `code --install-extension`、`code --list-extensions`、`code <fixture>` 自动 smoke
- 当前会话环境缺少可连接的 VS Code IPC server，命令返回 `ENOENT /run/user/1000/vscode-ipc-*.sock`

说明：

- 这不再是“未尝试”
- 当前阻断点是运行环境，而不是脚本或参数未知
- 文档应把这部分从模糊 `pending` 收敛成“已执行但环境阻断”

### 7. 当前有效文档入口存在重复副本，状态未对齐

当前状态：

- `docs/user-manual.md` 与 `docs/USER_MANUAL.md` 同时存在
- `docs/developer-guide.md` 与 `docs/DEVELOPER_GUIDE.md` 同时存在
- 至少大写版手册/指南仍保留明显旧口径，如旧版本号、旧安装方式、`完全支持`、`100%兼容`

说明：

- 这会让后续 review、release check、人工引用时混淆当前口径
- 重复入口本身已经成为“文档未对齐”的一部分

### 8. 源码注释仍有少量绝对兼容表述，和当前证据分级未对齐

已确认位置：

- `src/models/LACFileFormat.ts`
- `src/models/UnifiedDataFormat.ts`
- `src/services/DataExportService.ts`

说明：

- 当前有效文档已明确不能宣称 `.lac 100% 兼容`
- 源码注释若继续保留“100%兼容”“完全兼容原版格式”，会与外部文档口径冲突

## 本次建议同步到当前有效文档的修正

本次审计建议只做收口型修正，不新增范围：

1. 把 `README.md`、`docs/功能状态矩阵.md`、`docs/tests/README.md` 的“最近一次验证时间”更新到 2026-05-02，并明确今天 fresh 验证的是 `typecheck`、`typecheck:strict`、`lint`、`webview unit`、`decoders`、导出专项、quick、`package:dry`。
2. 把本审计文档加入 `docs/文档状态索引.md`，作为当前有效的专项审计入口。
3. 在 `docs/文档状态索引.md` 中显式标注 `docs/USER_MANUAL.md`、`docs/DEVELOPER_GUIDE.md` 为历史重复副本，避免与当前有效文档混用。
4. 将源码中的“100%兼容 / 完全兼容原版格式”注释降级为“按原版结构对齐，仍需真实样本或往返证据”。

## 当前结论

截至 2026-05-02，项目的软件主链路没有回退，4 月底形成的 Beta / 工程整备基线仍成立，并且 2026-05-01 已继续收口导出用户路径、映射 helper、文档门禁和 smoke 记录模板。

当前真正未完成的，不是“这些功能还没开发”，而是以下几类对齐工作还没收口：

- 真实硬件证据对齐
- VSIX GUI smoke 对齐
- 重复文档入口对齐
- 源码注释口径对齐

因此，后续整理和发布口径应优先围绕“对齐”而不是“继续宣称新增完成度”展开。
