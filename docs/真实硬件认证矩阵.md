# 真实硬件认证矩阵

更新时间：2026-04-28

本矩阵记录真实硬件能力的认证要求和当前证据。未完成认证的硬件不得在 README 或用户手册中描述为完整支持。

| 硬件 | 连接方式 | 检测口径 | 适配状态 | 认证等级 | 必测项 | 证据记录 |
| --- | --- | --- | --- | --- | --- | --- |
| Pico LogicAnalyzer | USB 串口 | VID/PID `1209/3020` | fixture 可测 | fixture | 8/16/24 通道采集、Edge/Fast/Complex/Blast、Stop/Abort 后重采、Bootloader、Blink | 待项目 01 提交日志、fixture 和版本号 |
| 普通 Raspberry Pi Pico | USB 串口 | VID/PID `2E8A/0003`，必须再做版本握手 | 候选识别 | candidate | 版本握手确认返回逻辑分析器固件后才允许连接 | 当前只作为候选，不得自动认证 |
| Pico W | USB 串口 + TCP | TCP `4045` + Pico 版本握手 | framework + fixture 可测 | experimental | 网络配置、TCP 采集、电压状态、断线重连、串口回退 | 待项目 01/07 提交真实设备记录 |
| Pico 2 / V2 | USB 串口 | VID/PID + 版本握手 | 待硬件确认 | experimental | 版本握手、blast 频率、缓冲区上限、24 通道采集 | 待补设备与固件版本 |
| 多设备 2 台 | USB 串口级联 | 稳定身份键、序列号、ParentId、assignedIndex | mock 可测 | experimental | 主从触发、Tag 映射、通道合并、从设备失败恢复 | 已有稳定排序 mock，待真实设备记录 |
| 多设备 3-5 台 | USB 串口级联 | 稳定身份键 + 版本一致性 | 规划中 | experimental | 稳定排序、版本一致性、120 通道合并、长时间采集 | 需要硬件池 |
| Saleae Logic 8 | Logic 2 API | localhost/127.0.0.1 `10429` + Saleae API 枚举 | framework | experimental | 设备枚举、采样率协商、采集到统一 `CaptureSession` | 当前不得标注完整支持 |
| Saleae Logic Pro 16 | Logic 2 API | localhost/127.0.0.1 `10429` + Saleae API 枚举 | framework | experimental | 模拟/数字通道能力识别、错误恢复、格式转换 | 当前不得标注完整支持 |
| Rigol 逻辑分析能力 | SCPI / LAN | `5555/5025/111` + `*IDN?` 包含 Rigol | framework | experimental | `*IDN?`、通道能力、采样率、数据块解析 | 需按型号拆分 |
| Siglent 逻辑分析能力 | SCPI / LAN | `5555/5025/111` + `*IDN?` 包含 Siglent | framework | experimental | `*IDN?`、数据读取、触发状态、断线恢复 | 需按型号拆分 |
| sigrok 支持设备 | `sigrok-cli` | `sigrok-cli --scan` | framework | experimental | 外部依赖检测、采集格式转换、错误码映射 | 需列出已验证设备清单 |

## 自动化验证路径

| 类别 | 命令或路径 | 验证内容 |
| --- | --- | --- |
| USB Pico 识别 | `npm exec -- jest tests/unit/drivers/HardwareDriverManager.business.test.ts --runInBand` | VID/PID `1209/3020` 精准识别，普通 Pico 降级为待握手候选 |
| 网络发现口径 | `NetworkDetector.getDefaultPorts()` 单元测试 | 统一端口为 `4045/10429/5555/5025/111` |
| Pico TCP 候选 | `NetworkDetector.fromProbeResult()` 单元测试 | 版本握手通过为高置信设备，端口开放仅为待验证候选 |
| 认证矩阵口径 | 本文档 + `docs/worktrees/07-device-discovery-certification.md` | 明确区分 framework、fixture、candidate、experimental |

## 认证记录模板

每次真实硬件认证必须追加以下信息：

| 字段 | 内容 |
| --- | --- |
| 日期 | YYYY-MM-DD |
| 操作系统 | Windows / macOS / Linux 版本 |
| 设备型号和序列号 | 真实型号、固件版本、序列号可脱敏 |
| 扩展版本和 commit | VSIX 版本、Git commit |
| 命令或操作路径 | VSCode 命令、CLI 命令或测试脚本 |
| 采集配置 | 频率、pre/post、通道、触发 |
| 结果文件 | `.lac`、CSV、日志、截图路径 |
| 结论 | 通过 / 失败 / 阻塞，并链接问题 |

## 认证记录 Schema

数据库字段位置：`DeviceCompatibilityEntry.testStatus.certificationEvidence`。

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `evidenceLevel` | `mock` / `fixture` / `hardware` | 是 | 当前条目最高证据等级。 |
| `records[].evidenceId` | string | 是 | 稳定记录 ID。 |
| `records[].date` | `YYYY-MM-DD` | 是 | 执行认证或 fixture 生成日期。 |
| `records[].operatingSystem` | string | 是 | 真实系统版本；fixture 可写 `CI fixture`。 |
| `records[].deviceModel` | string | 是 | 设备型号。 |
| `records[].firmwareVersion` | string | 真实硬件必填 | 固件版本或握手返回版本。 |
| `records[].serialNumber` | string | 真实硬件建议 | 可脱敏。 |
| `records[].extensionVersion` | string | 建议 | VSIX 或 package 版本。 |
| `records[].commit` | string | 真实硬件必填 | Git commit。 |
| `records[].commandOrPath` | string | 是 | 命令、VSCode 操作路径或测试脚本。 |
| `records[].captureConfig` | object | 采集证据必填 | `sampleRateHz`、`preTriggerSamples`、`postTriggerSamples`、`channels`、`trigger`、`mode`。 |
| `records[].resultFiles[]` | object[] | 是 | 路径、文件类型和 hash；真实硬件通过证据必须至少有一个带 `sha256` 的结果文件。 |
| `records[].result` | `pass` / `fail` / `blocked` / `pending` | 是 | 认证结论。 |
| `downgradeReason` | string | 条件必填 | 状态被降级时记录原因。 |

## 证据等级与降级规则

| 证据等级 | 含义 | 允许的最高认证等级 |
| --- | --- | --- |
| `mock` | 仅 mock、代码路径或静态声明；不证明设备可采集 | `experimental` 或 `community` |
| `fixture` | 有协议 fixture、golden 或离线回放；不含真实设备采集 | `fixture` |
| `hardware` | 真实设备运行并保存可追溯结果文件 hash | `verified` 或 `certified` |

规则：

- `certified` 和 `verified` 必须至少有一条 `hardware + pass` 记录，并包含系统、设备型号、固件版本、commit、采集配置、结果文件 hash。
- 缺少真实硬件通过证据时，数据库校验会把 `certified/verified` 降级；有 fixture 通过记录时降为 `fixture`，否则降为 `experimental` 或 `community`。
- 普通 Raspberry Pi Pico 这类只被 VID/PID 命中的设备必须保持 `candidate`，直到版本握手证明运行逻辑分析器固件。
- README、用户手册和发布说明只能引用本矩阵中 `hardware` 证据已经通过的真实硬件能力。
